# Kubernetes Service Manifest Template for LearnPyByAI Microservices
# This template shows the structure for deploying services with Dapr sidecars

apiVersion: v1
kind: Service
metadata:
  name: <SERVICE_NAME>
  namespace: default
  labels:
    app: <SERVICE_NAME>
spec:
  selector:
    app: <SERVICE_NAME>
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: <SERVICE_NAME>
  namespace: default
  labels:
    app: <SERVICE_NAME>
spec:
  replicas: 1
  selector:
    matchLabels:
      app: <SERVICE_NAME>
  template:
    metadata:
      labels:
        app: <SERVICE_NAME>
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "<APP_ID>"
        dapr.io/app-port: "8000"
        dapr.io/app-protocol: "http"
        dapr.io/config: "learnpybyai-config"
        dapr.io/log-level: "info"
        dapr.io/enable-metrics: "true"
        dapr.io/metrics-port: "9090"
    spec:
      containers:
      - name: <SERVICE_NAME>
        image: <IMAGE>
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: PORT
          value: "8000"
        - name: ENVIRONMENT
          value: "development"
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 250m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
