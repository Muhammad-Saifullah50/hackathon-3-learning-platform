version: '3.8'

services:
  mailhog:
    image: mailhog/mailhog:latest
    container_name: learnpybyai_mailhog
    ports:
      - "1025:1025"  # SMTP server
      - "8025:8025"  # Web UI
    networks:
      - learnpybyai_network

networks:
  learnpybyai_network:
    driver: bridge
